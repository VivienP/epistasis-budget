# Notebooks

`gb1_demo.ipynb` is generated in **Week 3** (see [`../docs/ROADMAP.md`](../docs/ROADMAP.md)). It renders
the headline figure — epistasis-map recovery vs budget B for info-optimal / fitness-greedy / random,
with bootstrap 95% CIs — from a saved `epibudget validate` report, so the main result is reproducible
end-to-end.

Until then this directory is intentionally empty of notebooks. The
[`reproduce-gb1-demo`](../.claude/skills/reproduce-gb1-demo/SKILL.md) skill documents the full pipeline.
